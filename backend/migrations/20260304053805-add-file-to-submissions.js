'use strict';

module.exports = {
  up: async (queryInterface, Sequelize) => {
    // Columns are already included in the Submission model creation
  },

  down: async (queryInterface, Sequelize) => {
    // Nothing to revert
  }
};
